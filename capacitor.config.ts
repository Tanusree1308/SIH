import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bovilens.app',
  appName: 'BOVILENS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
